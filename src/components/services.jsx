import React from "react";

export const Services = (props) => {
  return (
    <div id="services" className="text-center">
      <div className="container">
        <div className="section-title">
          <h2>NOSSOS SERVIÇOS</h2>
          <p style={{ fontWeight: "bold", color: "#ffff" }}>
            Explore as realizações que nos definem, descubra as ações que transformam vidas e testemunhe o impacto positivo que deixamos em nossa jornada de amor e serviço.
          </p>
        </div>
        <div className="row">
          {props.data
            ? props.data.map((d, i) => (
              <div key={`${d.name}-${i}`} className="col-md-4" style={{ height: '44%' }}>
                {" "}
                <i className={d.icon}></i>
                <div className="service-desc">
                  <h3>{d.name}</h3>
                  <p>{d.text}</p>
                </div>
              </div>
            ))
            : "loading"}
        </div>
      </div>
    </div>
  );
};
